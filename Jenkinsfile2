pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Perform build steps here
                sh 'echo "pr changes"'
                

            }
        }
        
        
    }
}
